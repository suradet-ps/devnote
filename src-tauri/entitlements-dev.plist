<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!--
      Development entitlements — no sandbox so that Finder "Open With"
      and file associations work during local testing. macOS does not
      pass the file path to a sandboxed app that is only ad-hoc signed;
      the user would need a paid Developer ID to make the sandbox +
      file-association combination work for distribution. See
      `entitlements.plist` for the production/sandboxed variant.
    -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
    <key>com.apple.security.files.downloads.read-write</key>
    <true/>
    <key>com.apple.security.network.client</key>
    <true/>
</dict>
</plist>
